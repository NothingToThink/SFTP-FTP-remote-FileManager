Файловый менеджер с интерфейсом под SFTP/FTP.

Сборка под Windows:
запустить `build_exe.bat`

Известные баги:
- переходить в папки даблкликом тяжело, часто вместо этого срабатывает переименование папки
